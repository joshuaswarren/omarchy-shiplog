// Shiplog — panel entry point (the day's log, grouped by repo).
// SPECIFICATION PHASE — not yet implemented. See docs/REQUIREMENTS.md.
import QtQuick

Item {
    id: root

    function open(payloadJson) {}
    function close() {}
}
